@object: obj.docker.stack.monitoring
@extends: class.compute.workload.docker.stack
@title: Monitoring Stack
@description: Prometheus + Grafana + Alertmanager + Loki observability stack
@version: 1.0.0

properties:
  stack_name: monitoring
  compose_version: "3.8"

shared_networks:
  - name: monitoring
    driver: bridge
