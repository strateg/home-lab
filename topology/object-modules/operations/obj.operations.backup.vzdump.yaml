@object: obj.operations.backup.vzdump
@extends: class.operations.backup
@version: 1.0.0
@description: Proxmox VE vzdump LXC/VM snapshot backup

properties:
  method: vzdump
  destination_type: proxmox_storage

  defaults:
    mode: snapshot
    compression: zstd
    keep_last: 3
    keep_daily: 7
    keep_weekly: 4

capabilities:
  - cap.operations.backup.snapshot
