object: obj.storage.pool.local
class_ref: class.storage.pool
version: 1.0.0
description: Local directory storage on Proxmox

properties:
  name: local
  type: dir
  path: /var/lib/vz
  content_types: [vztmpl, iso, backup, snippets]
  shared: false

enabled_capabilities:
  - cap.storage.pool.local
