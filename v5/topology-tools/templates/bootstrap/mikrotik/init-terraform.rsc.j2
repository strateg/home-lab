# Baseline RouterOS bootstrap script for {{ instance_id }}
:log info "Initialize Terraform API user (placeholder)"
# /user add name=terraform group=full password=<TODO_PASSWORD>
# /ip service enable api-ssl
