schema_version: 1
plugins:
  - id: object_network.validator_json.ethernet_cable_endpoints
    kind: validator_json
    entry: plugins/ethernet_cable_endpoint_validator.py:EthernetCableEndpointValidator
    api_version: "1.x"
    stages: [validate]
    order: 243
    depends_on:
      - base.validator.references
      - base.validator.ethernet_port_inventory
    timeout: 30
    config: {}
    config_schema:
      type: object
      properties: {}
      required: []
    description: Validates ethernet data-link endpoints and linkage to a created data-channel instance.
