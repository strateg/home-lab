version: 1
description: Router capability packs for v5 topology

packs:
  # ==========================================================================
  # HOME GATEWAY PACK
  # For: MikroTik Chateau LTE7 AX
  # Role: Central home gateway with WiFi 6, LTE failover, containers
  # ==========================================================================

  - id: pack.router.home_gateway
    title: Home Gateway Router Pack
    class_ref: class.router
    description: |
      Full-featured home gateway pack with advanced routing, WiFi 6,
      LTE failover, container support, and comprehensive services.
      Suitable for MikroTik and similar prosumer/small business routers.

    target_devices:
      - MikroTik Chateau LTE7 AX
      - MikroTik hAP ac³
      - Similar prosumer routers

    capabilities:
      # Core routing (always included)
      - cap.net.interface.ethernet
      - cap.net.l3.routing.static
      - cap.net.l3.routing.policy.pbr
      - cap.net.l3.translation.snat
      - cap.net.l3.translation.dnat
      - cap.net.l3.security.firewall.stateful
      - cap.net.l3.security.firewall.l7_filtering

      # WAN (multi-WAN with failover)
      - cap.net.l3.uplink.multi_uplink
      - cap.net.l3.uplink.failover
      - cap.net.interface.wifi
      - cap.net.interface.lte
      - cap.net.interface.pppoe

      # VPN (server + client)
      - cap.net.overlay.vpn.wireguard.server
      - cap.net.overlay.vpn.wireguard.client
      - cap.net.overlay.vpn.ipsec

      # WiFi (WiFi 6 with guest network)
      - cap.net.interface.wifi.dual_band
      - cap.net.interface.wifi.wifi6
      - cap.net.interface.wifi.mu_mimo
      - cap.net.interface.wifi.beamforming
      - cap.net.l2.segmentation.guest_isolation

      # Services (full suite)
      - cap.net.service.dhcp.server
      - cap.net.service.dns.resolver
      - cap.net.service.ntp.server
      - cap.net.service.snmp
      - cap.net.service.dyndns

      # Advanced features
      - cap.net.l3.qos.advanced
      - cap.net.l2.segmentation.vlan.8021q
      - cap.net.platform.containers
      - cap.net.observability.netflow
      - cap.net.management.api.rest

    use_cases:
      - Central home network gateway
      - Small office router
      - Lab/homelab central router
      - Self-hosted services gateway

    typical_roles:
      - central_gateway
      - home_router
      - lab_gateway

  # ==========================================================================
  # TRAVEL VPN PACK
  # For: GL.iNet Slate AX
  # Role: Portable VPN gateway for travel with repeater mode
  # ==========================================================================

  - id: pack.router.travel_vpn
    title: Travel VPN Gateway Pack
    class_ref: class.router
    description: |
      Compact travel router pack optimized for VPN client mode, WiFi repeater,
      and portable use. Features multi-instance VPN, policy routing, and
      hotel WiFi connectivity. Suitable for GL.iNet and similar travel routers.

    target_devices:
      - GL.iNet Slate AX (GL-AXT1800)
      - GL.iNet Flint (GL-AX1800)
      - GL.iNet Beryl AX (GL-MT3000)
      - Similar portable/travel routers

    capabilities:
      # Core routing
      - cap.net.interface.ethernet
      - cap.net.l3.routing.static
      - cap.net.l3.translation.snat
      - cap.net.l3.translation.dnat
      - cap.net.l3.security.firewall.stateful
      - cap.net.l3.security.firewall.zone_policy

      # VPN (focus on CLIENT mode!)
      - cap.net.overlay.vpn.wireguard.client      # Primary: connect to home
      - cap.net.overlay.vpn.wireguard.server      # Secondary: accept connections
      - cap.net.overlay.vpn.openvpn.client        # Alternative VPN
      - cap.net.overlay.vpn.multi_instance        # Multiple VPN simultaneously
      - cap.net.overlay.vpn.policy_routing        # Per-client/domain VPN routing

      # WiFi (with repeater mode!)
      - cap.net.interface.wifi
      - cap.net.interface.wifi.dual_band
      - cap.net.interface.wifi.wifi6
      - cap.net.interface.wifi.mu_mimo
      - cap.net.l2.segmentation.guest_isolation
      - cap.net.interface.wifi.repeater_mode       # KEY: Connect to hotel WiFi

      # Services (privacy-focused)
      - cap.net.service.dhcp.server
      - cap.net.service.dns.resolver
      - cap.net.service.dns.dot                   # Encrypted DNS
      - cap.net.service.dns.doh                   # Alternative encrypted DNS
      - cap.net.service.dns.filtering               # AdGuard Home
      - cap.net.service.dyndns

      # QoS (optimized for variable connections)
      - cap.net.l3.qos.sqm                   # SQM for hotel WiFi

      # Advanced
      - cap.net.l2.segmentation.vlan.8021q

    use_cases:
      - Travel VPN gateway (hotel, airport, cafe)
      - Remote work secure network
      - Portable home network bridge
      - Privacy-focused portable router

    typical_roles:
      - travel_vpn_gateway
      - portable_router
      - remote_bridge

    notes: |
      This pack emphasizes:
      - VPN CLIENT over server (connect TO home, not FROM)
      - WiFi repeater mode (upstream via WiFi, not just Ethernet)
      - Multi-instance VPN (simultaneous tunnels)
      - Privacy features (DoT, DoH, AdGuard)
      - Compact form factor compatibility

  # ==========================================================================
  # ENTERPRISE ROUTING PACK
  # For: Future enterprise-grade routers
  # Role: Advanced routing with BGP, OSPF, VRF for enterprise networks
  # ==========================================================================

  - id: pack.router.enterprise
    title: Enterprise Routing Pack
    class_ref: class.router
    description: |
      Enterprise-grade routing pack with BGP, OSPF, VRF, and advanced
      traffic engineering. Suitable for MikroTik CCR series, Ubiquiti
      EdgeRouter, and similar enterprise routers.

    target_devices:
      - MikroTik CCR series
      - MikroTik RB5009
      - Ubiquiti EdgeRouter
      - Cisco ISR (running OpenWRT)

    capabilities:
      # Advanced routing protocols
      - cap.net.interface.ethernet
      - cap.net.l3.routing.static
      - cap.net.l3.routing.dynamic.bgp              # AS-level routing
      - cap.net.l3.routing.dynamic.ospf             # IGP routing
      - cap.net.l3.routing.ecmp             # Load balancing
      - cap.net.l3.routing.policy.pbr              # Policy routing

      # NAT & Firewall
      - cap.net.l3.translation.snat
      - cap.net.l3.translation.dnat
      - cap.net.l3.security.firewall.stateful
      - cap.net.l3.security.firewall.zone_policy

      # VPN (server-focused)
      - cap.net.overlay.vpn.wireguard.server
      - cap.net.overlay.vpn.ipsec

      # Services
      - cap.net.service.dhcp.server
      - cap.net.service.dhcp.relay
      - cap.net.service.dns.resolver
      - cap.net.service.snmp

      # Advanced features
      - cap.net.interface.fiber
      - cap.net.l2.segmentation.vlan.8021q
      - cap.net.l3.segmentation.vrf                      # Virtual routing domains
      - cap.net.l3.qos.advanced             # HTB, queue trees
      - cap.net.observability.netflow                  # Traffic analysis
      - cap.net.management.api.rest                 # Automation

    use_cases:
      - Enterprise network edge
      - Data center routing
      - ISP customer edge (CE)
      - Multi-tenant routing

    typical_roles:
      - enterprise_router
      - datacenter_gateway
      - isp_edge

    notes: |
      This pack is currently PLANNED for future enterprise router additions.
      No active instances use this pack yet.

# Metadata
metadata:
  total_packs: 3
  active_packs: 2  # home_gateway, travel_vpn
  planned_packs: 1 # enterprise

  pack_comparison:
    home_gateway:
      capabilities: 25
      complexity: high
      target: prosumer/small_business
      form_factor: desktop
      primary_role: central_gateway

    travel_vpn:
      capabilities: 18
      complexity: medium
      target: consumer/traveler
      form_factor: portable
      primary_role: travel_vpn_gateway

    enterprise:
      capabilities: 18
      complexity: very_high
      target: enterprise/isp
      form_factor: rack_mount
      primary_role: enterprise_router
