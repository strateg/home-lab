---
# Local network inventory overrides
# Use this inventory when running playbooks from local network
all:
  children:
    devices:
      hosts:
        rtr-mikrotik-chateau:
          ansible_host: 192.168.88.1
          mikrotik_api_port: 443
