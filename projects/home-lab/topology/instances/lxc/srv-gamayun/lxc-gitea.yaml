@instance: lxc-gitea
@extends: obj.proxmox.lxc.debian12.gitea
@group: lxc
@version: 1.1.0
source_id: lxc-gitea
notes: Gitea self-hosted Git service

vmid: 203
hostname: gitea
@description: Gitea self-hosted Git service
tags:
  - git
  - production

host_ref: srv-gamayun

os_refs:
  - inst.os.debian.12.proxmox.lxc

# Instance-specific overrides (inherits rest from host via @on)
resource_profile_ref: rp.lxc.standard

boot:
  startup_order: 21

storage:
  rootfs:
    size_gb: 8

network:
  ip: 10.0.30.40/24

cloudinit:
  user: root

ansible:
  playbook: gitea.yml
