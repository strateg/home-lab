@instance: lxc-prometheus
@extends: obj.proxmox.lxc.debian12.prometheus
@group: lxc
@version: 1.1.0
source_id: lxc-prometheus
notes: Prometheus metrics collection

vmid: 206
hostname: prometheus
@description: Prometheus metrics collection
tags:
  - monitoring
  - production

host_ref: srv-gamayun

os_refs:
  - inst.os.debian.12.proxmox.lxc

# Instance-specific overrides (inherits rest from host via @on)
resource_profile_ref: rp.lxc.balanced

boot:
  startup_order: 29

storage:
  rootfs:
    size_gb: 16

network:
  vlan_ref: inst.vlan.servers
  host: 70

cloudinit:
  user: root

ansible:
  playbook: prometheus.yml
