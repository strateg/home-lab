@instance: lxc-nginx-proxy
@extends: obj.proxmox.lxc.debian12.nginx
@group: lxc
@version: 1.1.0
source_id: lxc-nginx-proxy
notes: Nginx reverse proxy

vmid: 207
hostname: nginx-proxy
@description: Nginx reverse proxy
tags:
  - proxy
  - production

host_ref: srv-gamayun

os_refs:
  - inst.os.debian.12.proxmox.lxc

# Instance-specific overrides (inherits rest from host via @on)
resource_profile_ref: rp.lxc.small

boot:
  startup_order: 15

storage:
  rootfs:
    size_gb: 4

network:
  ip: 10.0.30.80/24

cloudinit:
  user: root

ansible:
  playbook: nginx-proxy.yml
