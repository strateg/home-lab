@instance: lxc-grafana
@extends: obj.proxmox.lxc.debian12.grafana
@group: lxc
@version: 1.1.0
source_id: lxc-grafana
status: mapped
notes: Grafana observability dashboards

vmid: 205
hostname: grafana
@description: Grafana observability dashboards
tags:
  - monitoring
  - production

host_ref: srv-gamayun

os_refs:
  - inst.os.debian.12.proxmox.lxc

# Instance-specific overrides (inherits rest from host via @on)
resource_profile_ref: rp.lxc.small

boot:
  startup_order: 30

storage:
  rootfs:
    size_gb: 8

network:
  ip: 10.0.30.60/24

cloudinit:
  user: root

ansible:
  playbook: grafana.yml
