@instance: lxc-docker
@extends: obj.proxmox.lxc.debian12.docker
@group: lxc
@version: 1.1.0
source_id: lxc-docker
status: mapped
notes: Docker container host

vmid: 208
hostname: docker-host
@description: Docker container host
tags:
  - docker
  - production

host_ref: srv-gamayun

os_refs:
  - inst.os.debian.12.proxmox.lxc

# Instance-specific overrides (inherits rest from host via @on)
resource_profile_ref: rp.lxc.standard

boot:
  startup_order: 25

storage:
  rootfs:
    size_gb: 20

network:
  ip: 10.0.30.90/24

cloudinit:
  user: root

ansible:
  playbook: docker.yml
