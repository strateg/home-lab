@instance: inst.vlan.management
@extends: obj.network.vlan.management
@group: network
@version: 1.0.0
source_id: net-management
status: modeled
notes: Management VLAN is staged in topology; currently not configured on rtr-mikrotik-chateau
bridge_ref: inst.bridge.vmbr0
trust_zone_ref: inst.trust_zone.management

ip_allocations:
  - device_ref: srv-gamayun
    ip: 10.0.99.1/24
    purpose: hypervisor management
