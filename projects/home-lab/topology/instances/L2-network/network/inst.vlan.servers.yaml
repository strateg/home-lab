@instance: inst.vlan.servers
@extends: obj.network.vlan.servers
group: network
@layer: L2
@version: 1.0.0
source_id: net-servers
status: modeled
notes: Server VLAN is staged in topology; currently not configured on rtr-mikrotik-chateau
bridge_ref: inst.bridge.vmbr0
trust_zone_ref: inst.trust_zone.servers

ip_allocations:
  - device_ref: srv-orangepi5
    ip: 10.0.30.5/24
    purpose: docker host
