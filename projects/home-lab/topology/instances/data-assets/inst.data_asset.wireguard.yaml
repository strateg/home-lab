@instance: inst.data_asset.wireguard
@extends: obj.storage.data_asset.config
@group: data-assets
@version: 1.0.0
source_id: data-wireguard
status: mapped
notes: WireGuard keys and peer configuration (stored in RouterOS)
host_ref: mikrotik-chateau
criticality: critical
backup_policy: daily
