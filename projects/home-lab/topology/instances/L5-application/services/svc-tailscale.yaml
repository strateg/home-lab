instance: svc-tailscale
object_ref: obj.service.tailscale
group: services
layer: L5
version: 1.0.0
source_id: svc-tailscale
status: mapped
notes: Tailscale mesh VPN (container on MikroTik)

trust_zone_ref: inst.trust_zone.user
vpn_type: tailscale
description: Tailscale mesh VPN for zero-config access
critical: true

runtime:
  type: docker
  target_ref: rtr-mikrotik-chateau
  image: tailscale/tailscale
  resources:
    memory_mb: 50
    memory_limit_mb: 100

features:
  - Zero-config mesh VPN
  - MagicDNS
  - Exit node capability
  - Subnet routing

data_asset_refs:
  - inst.data_asset.tailscale
