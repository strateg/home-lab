# DNS/DHCP Overview

Generated by `base.generator.docs`.

{% set services_rows = projection.get('services', []) %}
{% set networks = network.get('networks', []) %}

## DNS-related Services

| Service | Class | Runtime Target | Network Binding | Status |
| --- | --- | --- | --- | --- |
{% for row in services_rows if 'service.dns' in (row.class_ref or '') -%}
| {{ row.instance_id }} | {{ row.class_ref }} | {{ row.runtime_target_ref or '-' }} | {{ row.runtime_network_ref or '-' }} | {{ row.status }} |
{% endfor %}

## DHCP Ranges by Network

| Network | DHCP Range | Managed By | Trust Zone | Status |
| --- | --- | --- | --- | --- |
{% for row in networks -%}
| {{ row.instance_id }} | {{ row.dhcp_range or '-' }} | {{ row.managed_by_ref or '-' }} | {{ row.trust_zone_ref or '-' }} | {{ row.status }} |
{% endfor %}
