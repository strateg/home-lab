# Storage Topology

Generated by `base.generator.docs`.

{% set pools = storage.get('storage_pools', []) %}
{% set assets = storage.get('data_assets', []) %}
{% set chains = storage.get('mount_chains', []) %}

## Storage Pools

| Pool | Host | Status | Notes |
| --- | --- | --- | --- |
{% for row in pools -%}
| {{ row.instance_id }} | {{ row.host_ref or '-' }} | {{ row.status }} | {{ row.notes or '-' }} |
{% endfor %}

## Data Assets

| Asset | Engine | Host | Criticality | Backup Policy | Status |
| --- | --- | --- | --- | --- | --- |
{% for row in assets -%}
| {{ row.instance_id }} | {{ row.engine or '-' }} | {{ row.host_ref or '-' }} | {{ row.criticality or '-' }} | {{ row.backup_policy or '-' }} | {{ row.status }} |
{% endfor %}

## Backup/Storage Chains

| Operation | Target | Storage | Data Asset | Mode | Schedule |
| --- | --- | --- | --- | --- | --- |
{% for row in chains -%}
| {{ row.operation_id }} | {{ row.target_ref or '-' }} | {{ row.storage_ref or '-' }} | {{ row.data_asset_ref or '-' }} | {{ row.mode or '-' }} | {{ row.schedule or '-' }} |
{% endfor %}
