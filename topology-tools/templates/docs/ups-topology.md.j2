# UPS Topology

Generated by `base.generator.docs`.

{% set ups_inventory = operations.get('ups_inventory', []) %}
{% set backups = operations.get('backup_policies', []) %}

## UPS Inventory

| UPS | External Source | Max Watts | Status | Notes |
| --- | --- | ---: | --- | --- |
{% for row in ups_inventory -%}
| {{ row.instance_id }} | {{ row.external_source or '-' }} | {{ row.max_watts if row.max_watts is not none else '-' }} | {{ row.status }} | {{ row.notes or '-' }} |
{% endfor %}

## Backup Policies

| Backup | Target | Data Asset | Storage | Mode | Schedule |
| --- | --- | --- | --- | --- | --- |
{% for row in backups -%}
| {{ row.instance_id }} | {{ row.target_ref or '-' }} | {{ row.data_asset_ref or '-' }} | {{ row.storage_ref or '-' }} | {{ row.mode or '-' }} | {{ row.schedule or '-' }} |
{% endfor %}
