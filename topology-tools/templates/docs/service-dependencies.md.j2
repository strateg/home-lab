# Service Dependencies

Generated by `base.generator.docs`.

{% set deps = service_dependencies %}

```mermaid
graph LR
{% for row in deps %}
  {{ row.service_id | replace('.', '_') | replace('-', '_') }} --> {{ row.depends_on | replace('.', '_') | replace('-', '_') | replace('@', '_') }}
{% endfor %}
```

| Service | Depends On |
| --- | --- |
{% for row in deps -%}
| {{ row.service_id }} | {{ row.depends_on }} |
{% endfor %}

{% if not deps %}
_No explicit service dependencies were found._
{% endif %}
