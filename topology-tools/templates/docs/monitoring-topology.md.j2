# Monitoring Topology

Generated by `base.generator.docs`.

{% set healthchecks = operations.get('healthchecks', []) %}
{% set alerts = operations.get('alerts', []) %}

## Healthchecks

| Healthcheck | Target | Interval | Timeout | Critical | Status |
| --- | --- | --- | --- | --- | --- |
{% for row in healthchecks -%}
| {{ row.instance_id }} | {{ row.target_ref or '-' }} | {{ row.interval or '-' }} | {{ row.timeout or '-' }} | {{ row.critical }} | {{ row.status }} |
{% endfor %}

## Alerts

| Alert | Severity | Channels | Status |
| --- | --- | --- | --- |
{% for row in alerts -%}
| {{ row.instance_id }} | {{ row.severity or '-' }} | {{ row.channels | join(', ') if row.channels else '-' }} | {{ row.status }} |
{% endfor %}
