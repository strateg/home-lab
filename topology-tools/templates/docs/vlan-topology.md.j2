# VLAN Topology

Generated by `base.generator.docs`.

{% set vlans = security.get('vlans', []) %}
{% set firewall_policies = security.get('firewall_policies', []) %}

| VLAN Instance | Trust Zone | Managed By | DHCP Range | Status |
| --- | --- | --- | --- | --- |
{% for row in vlans -%}
| {{ row.instance_id }} | {{ row.trust_zone_ref or '-' }} | {{ row.managed_by_ref or '-' }} | {{ row.dhcp_range or '-' }} | {{ row.status }} |
{% endfor %}

## Firewall Policies

| Policy | Chain | Managed By | Status | Notes |
| --- | --- | --- | --- | --- |
{% for row in firewall_policies -%}
| {{ row.instance_id }} | {{ row.chain or '-' }} | {{ row.managed_by_ref or '-' }} | {{ row.status }} | {{ row.notes or '-' }} |
{% endfor %}
