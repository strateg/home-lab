# Trust Zones

Generated by `base.generator.docs`.

{% set trust_zones = security.get('trust_zones', []) %}
{% set bindings = security.get('zone_network_bindings', {}) %}

| Trust Zone | Networks | Status | Notes |
| --- | --- | --- | --- |
{% for row in trust_zones -%}
| {{ row.instance_id }} | {{ bindings.get(row.instance_id, []) | join(', ') if bindings.get(row.instance_id, []) else '-' }} | {{ row.status }} | {{ row.notes or '-' }} |
{% endfor %}
