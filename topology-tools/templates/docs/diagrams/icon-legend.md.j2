# Icon Legend

Generated by `base.generator.diagrams`.

Current icon mode: `{{ icon_mode }}`
{% if mermaid_icon_runtime_hint %}

> {{ mermaid_icon_runtime_hint }}
{% endif %}

## Device Classes

```mermaid
flowchart TB
{% for entry in legend_device_classes %}
{% if use_mermaid_icons %}
  dev_{{ entry.safe_id }}@{ icon: "{{ entry.icon }}", form: "rounded", label: "{{ entry.class_ref }}", pos: "b", h: 42 }
{% else %}
  dev_{{ entry.safe_id }}["{{ entry.class_ref }}\n{{ entry.icon }}"]
{% endif %}
{% endfor %}
```

| Class | Icon |
| --- | --- |
{% for entry in legend_device_classes -%}
| `{{ entry.class_ref }}` | `{{ entry.icon }}` |
{% endfor %}

## Trust Zones

```mermaid
flowchart LR
{% for zone in trust_zones %}
{% if use_mermaid_icons %}
  zone_{{ zone.safe_id }}@{ icon: "{{ zone.icon }}", form: "circle", label: "{{ zone.label }}", pos: "b", h: 42 }
{% else %}
  zone_{{ zone.safe_id }}["{{ zone.label }}\n{{ zone.icon }}"]
{% endif %}
{% endfor %}
```

| Zone | Icon |
| --- | --- |
{% for zone in trust_zones -%}
| `{{ zone.label }}` | `{{ zone.icon }}` |
{% endfor %}
