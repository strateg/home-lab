---
# PostgreSQL Database Server Playbook

- name: Configure PostgreSQL Database
  hosts: lxc_containers
  become: yes
  gather_facts: yes
  tags: [postgresql, database]

  roles:
    - role: postgresql
      when: "'postgresql' in inventory_hostname or 'database' in group_names"
