---
# Common Role - Handlers

- name: restart ssh
  systemd:
    name: ssh
    state: restarted

- name: restart chrony
  systemd:
    name: chrony
    state: restarted

- name: reload systemd
  systemd:
    daemon_reload: yes
