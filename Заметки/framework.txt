Проведи углублённый аналих adr0075 adr0076 с цеоью выработки лучшей арзитектуры для сземы framework + multiple projects   (1 : n).  Основная илея что текущий репозиторий является основным в плане разработки фреймворка с пмошью AI .  С этого репозитория строится артефакт franmework конкретной версии, который йсодержит только скрипты которые отвечают за сборку проекта и гнерацию результирующих артефактов terraform, ansible и т.д. Кажлый проект развивается в своём отдельном репозитории и framework артефакт подклбчается к проекту как зависимость. Framework не содердит тестов, adr, докуметации по разработке самого FRAMEWORK. оН СОДЕРЖИТ ТОЛЬКО ФЕНКЦИОНАЛ НУЖНЫЙ для CI/, classes, objects, plugins. Проекь содержит instances_ pluginns. Но проекта с зависимостями достаточно для генерации результирующих артефактов. Думай.

Нужно заметить что основной репозиторий содержит тестовый проект и топологию для того чтобы все находилось в одном месте и проект собирается с помощью тех же скриптов. В случае с topology execution environment все скрипты позволяющие собрать проект также рабочие и нужно продумать как зависимость интегрируется в проект чтобы скрипты компиляции всё также работали. Проект преобразуется в  classes object instances в одном root? Как будет происходить подключение framework на практике? Как будет проиходить обновление версии framework?

Категоризируй вспомгательнйй скрипты в папке topology-tools и реши что должно попасть в topology runtime environment а что должно остаться в репозитории разработки фреймворка

Хочу составить регистр скриптов чтобы Ai и человек понимали какоой скрипт за что отвечает и какой скрипт должен быть в framework artifact

Проанализируй текущее расположение плагинов , прочитай adr0063, прочитай adr0081 и предложи лучшую модуляризацию в свете требований adr0081. Plugin discovery, отдельные артефакты с совместным расположением class-modules, class-plugins, object-modules, object plugins, возсожно стоит упаковывать в один артефакт и потом разворачивать в совмещенную структуру в  отдельном проекте. Анализируй, предлагай
